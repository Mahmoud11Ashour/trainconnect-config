server:
  port: 8083

spring:
  application:
    name: opportunityservice
  datasource:
    url: jdbc:mysql://localhost:3306/opportunity_db
    username: root
    password: 
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true