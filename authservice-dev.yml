server:
  port: 8085

spring:
  application:
    name: authservice
  datasource:
    url: jdbc:mysql://localhost:3306/auth_db
    username: root
    password: 
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
jwt:
  secret: secretkey