server:
  port: 8081

spring:
  application:
    name: studentservice

  datasource:
    url: jdbc:mysql://localhost:3306/student_db
    username: root
    password: 

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
